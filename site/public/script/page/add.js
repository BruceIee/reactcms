var app = app || {};

$().ready(function() {
    setup();
});

function setup() {
    console.log('in page add script');

    // setup composition dropdown combobox
    var compositionListUrl = '/data/compositions';
    
    
}

